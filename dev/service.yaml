apiVersion: v1
kind: Service
metadata:
  name: xchalle-bonus-playground-service
  namespace: gitlab
spec:
  selector:
    app: xchalle-bonus-playground
  ports:
  - name: http
    protocol: TCP
    port: 8889
    targetPort: 8888
